{
  "buildCommand": "cd Tree && npm install && npm run build",
  "outputDirectory": "Tree/dist",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/((?!api/.*).*)",
      "destination": "/index.html"
    }
  ]
}
